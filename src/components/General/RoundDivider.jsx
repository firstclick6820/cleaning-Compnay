import { Divider, styled } from '@mui/material';

const RoundDivider = styled(Divider)({
  width: '100%',
  height: '4px',
  borderRadius: '50%',
  backgroundColor: 'grey',
});

export default RoundDivider;
